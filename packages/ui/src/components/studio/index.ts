/**
 * Studio - Ready-to-use chat interface
 *
 * New architecture:
 * - Agent instance = one conversation
 * - Image = saved conversation snapshot
 */

export { Studio, type StudioProps } from "./Studio";
