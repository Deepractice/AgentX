/**
 * Bash Module — low-level command execution types.
 *
 * These types are used internally by AgentOS Shell implementations.
 * For tool injection, use @agentxjs/core/os instead.
 */

export type { BashOptions, BashProvider, BashResult } from "./types";
