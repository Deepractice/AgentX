# Lefthook configuration for AgentX
# https://lefthook.dev/configuration/

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-commit:
  commands:
    biome:
      glob: "*.{js,ts,jsx,tsx,json}"
      run: bun biome check --write {staged_files} && git add {staged_files}

pre-push:
  commands:
    check:
      run: bun biome check .
    # typecheck moved to CI — too slow for pre-push, causes SSH timeout
